# learnsquad recipe app

A simple recipe-sharing app starter with a React frontend and a Node/Express backend.

## Structure

- frontend: Vite + React app
- backend: Express API server

## Run locally

```bash
npm install
npm run dev
npm run dev:backend
```

The frontend runs on http://localhost:5173 and the backend on http://localhost:3000.
